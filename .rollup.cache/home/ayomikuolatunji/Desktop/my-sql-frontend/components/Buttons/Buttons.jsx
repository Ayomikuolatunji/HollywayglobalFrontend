import React from 'react';
var Buttons = function (_a) {
    var text = _a.text, className = _a.className, disabled = _a.disabled, type = _a.type, onClick = _a.onClick;
    return (<button type={"button" || type} className={"".concat(className)} disabled={disabled} onClick={onClick}>
      {text}
    </button>);
};
export default Buttons;
//# sourceMappingURL=Buttons.jsx.map