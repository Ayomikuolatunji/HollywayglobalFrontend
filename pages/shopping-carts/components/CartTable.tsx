import React, { useState } from "react";
import { cartItemTypes, productsCarts } from "../../../models";

export default function CartTable({ data }: any) {
  return <div className="w-full shadow-inner"></div>;
}
